import React from 'react'

function Laptop({laptop, key, brand, name}) {
    return (
        <div>
            {laptop} {name} {brand}

            <button></button>
        </div>
    )
}

export default Laptop
